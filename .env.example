# Postgres
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/observer

# Must match fx-alert NEXTAUTH_SECRET
NEXTAUTH_SECRET=generate-a-long-random-string

# Set true only for local debugging without tokens
# AUTH_DISABLED=false

# WebSocket URL returned to authenticated clients
WS_URL=ws://localhost:8000/ws/observe
